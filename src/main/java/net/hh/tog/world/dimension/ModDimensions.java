package net.hh.tog.world.dimension;

import net.fabricmc.fabric.mixin.registry.sync.RegistriesAccessor;
import net.hh.tog.TogMod;
import net.minecraft.registry.Registries;
import net.minecraft.registry.Registry;
import net.minecraft.registry.RegistryKey;
import net.minecraft.util.Identifier;
import net.minecraft.world.World;

public class ModDimensions {
    /*public static final RegistryKey<World> TOGDIM_DIMENSION_KEY = RegistryKey.of();

    private static World registerWorld(String name, World world) {
        return Registry.register(Registries.ITEM, new Identifier(TogMod.MOD_ID, name), world);
    }*/
}
